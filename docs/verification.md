# MVP 验收记录

验收日期：2026-09-20。测试数据使用独立的 `.runtime/browser-qa.db`，正式数据库不预置目标、项目或任务。

## 自动化与运行检查

- `python -m pytest backend/tests -q`：61 项通过。另有 2 条第三方测试依赖弃用警告，不影响测试结果。
- 前端生产构建与 TypeScript 检查通过；安装阶段 npm 审计报告 0 个已知漏洞。
- `pip check` 通过；Alembic 迁移执行成功，`alembic check` 未发现未迁移的模型变化。
- 完整运行 `setup.ps1 -DirectNetwork`，验证依赖安装、迁移与前端构建。
- 验证 `start.ps1`、`stop.ps1` 的后台启动、健康检查与停止流程。
- `backup.ps1` 成功生成 SQLite 在线备份并通过完整性检查。

后端测试覆盖重复周期、离线补齐、远期窗口、暂停恢复、修改例外、依赖与循环拒绝、任务完成和重开、版本冲突、提醒去重、日期边界、复盘与统计等行为。具体用例见 `backend/tests/`。

## 浏览器实际操作

- 创建目标、项目和里程碑，创建归属任务并修改日期与时间。
- 在日历日视图拖动任务，拉伸结束时间，重新打开编辑器确认保存值。
- 从日历任务编辑器标记完成；Today 完成率和项目已生成任务进度同步更新。
- 创建两天的每日重复规则，确认生成编号 1、2 的任务，日期与默认时间正确。
- 保存每日复盘的 75 分钟实际投入，其中 60 分钟归属项目；统计页显示项目时间与未分配时间。
- 检查 390 像素手机宽度布局，打开导航并切换页面。
- 本轮验收浏览器错误和警告日志为空。

## 边界

浏览器通知依赖页面运行与系统权限；未声称关闭网页后仍可靠推送。Google Calendar 和 AI 仅保留服务接口，尚未连接。进度比例来自已生成任务，不代表长期目标的真实完成百分比。本版本为单人本机应用，不包含公网认证或多用户支持。
